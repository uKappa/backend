/// <reference types="@qualweb/types" />
/// <reference types="@qualweb/types" />
import { Page } from 'puppeteer';
import { QualwebOptions, Execute } from '@qualweb/core';
import EvaluationRecord from './evaluationRecord.object';
import { HTMLValidationReport } from '@qualweb/html-validator';
declare class Evaluation {
    private readonly url;
    private readonly page;
    private readonly execute;
    constructor(url: string, page: Page, execute: Execute);
    evaluatePage(sourceHtml: string, options: QualwebOptions, validation?: HTMLValidationReport): Promise<EvaluationRecord>;
    private getEvaluator;
    private parseUrl;
    private init;
    private executeACT;
    private executeWCAG;
    private executeBP;
    private executeCounter;
    private detectIfUnwantedTabWasOpened;
}
export { Evaluation, EvaluationRecord };
//# sourceMappingURL=index.d.ts.map